const reverseList = require('./index');

test('206', () => {
  // expect(reverseList([1,2,3,4,5])).toStrictEqual([5,4,3,2,1]);
  // expect(reverseList([1,2])).toStrictEqual([2, 1]);
  // expect(reverseList([])).toStrictEqual([]);
});
