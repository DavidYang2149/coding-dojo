const levelOrder = require('./index');

test('102', () => {
  // expect(levelOrder([3,9,20,null,null,15,7])).toStrictEqual([[3],[9,20],[15,7]]);
  // expect(levelOrder([1])).toStrictEqual([1]);
  // expect(levelOrder([])).toStrictEqual([]);
});
